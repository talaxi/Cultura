import { StringNumberPair } from './string-number-pair.model';

describe('StringNumberPair', () => {
  it('should create an instance', () => {
    expect(new StringNumberPair()).toBeTruthy();
  });
});
