import { PinnacleConditions } from './pinnacle-conditions.model';

describe('PinnacleConditions', () => {
  it('should create an instance', () => {
    expect(new PinnacleConditions()).toBeTruthy();
  });
});
