import { EventSegmentCarryOverData } from './event-segment-carry-over-data.model';

describe('EventSegmentCarryOverData', () => {
  it('should create an instance', () => {
    expect(new EventSegmentCarryOverData()).toBeTruthy();
  });
});
