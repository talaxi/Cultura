import { EventAbilityData } from './event-ability-data.model';

describe('EventAbilityData', () => {
  it('should create an instance', () => {
    expect(new EventAbilityData()).toBeTruthy();
  });
});
