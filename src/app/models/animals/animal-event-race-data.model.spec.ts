import { AnimalEventRaceData } from './animal-event-race-data.model';

describe('AnimalEventRaceData', () => {
  it('should create an instance', () => {
    expect(new AnimalEventRaceData()).toBeTruthy();
  });
});
