import { IncubatorStatUpgrades } from './incubator-stat-upgrades.model';

describe('IncubatorStatUpgrades', () => {
  it('should create an instance', () => {
    expect(new IncubatorStatUpgrades()).toBeTruthy();
  });
});
