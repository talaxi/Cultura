import { AllTrainingTracks } from './all-training-tracks.model';

describe('AllTrainingTracks', () => {
  it('should create an instance', () => {
    expect(new AllTrainingTracks()).toBeTruthy();
  });
});
